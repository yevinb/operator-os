---
description: Nexa is powered by Cursor — all user business actions route through the Cursor engine.
alwaysApply: true
---

# Nexa ↔ Cursor

**Users talk to Nexa. Cursor controls everything.**

All chat, email, and business execution in `operator-os` flows through `backend/app/services/cursor_engine.py`. Do not bypass it.

When editing Nexa:
- Keep the Cursor agent tool loop (`send_email`, `execute_command`, `run_autopilot`)
- UI shows "Powered by Cursor"
- End users never use terminal/CLI — they use the web app only
